<Page
    x:Class="Application6.SecondPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Application6"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding SecondPageViewModel, Source={StaticResource ViewModelLocator}}"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundBrush}">
        <TextBlock Text="Second page" HorizontalAlignment="Center" VerticalAlignment="Top" Style="{StaticResource PageHeaderTextStyle}" />
        <Button Content="Navigate back" Command="{Binding GoBack}"/>
    </Grid>
</Page>